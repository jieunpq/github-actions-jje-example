name: dev_02_이벤트_종류-2025-06-12

on:
  push:
    paths:
      - '**.md'
    # branches:
    #   - dev_02_이벤트_종류-2025-06-12

jobs:
  daily-job:
    # main 브랜치에서만 실행
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Print current date
        run: date
